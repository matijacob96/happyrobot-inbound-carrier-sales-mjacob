steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - "build"
      - "-f"
      - "apps/api/Dockerfile"
      - "-t"
      - "gcr.io/${PROJECT_ID}/hr-api:latest"
      - "."
images:
  - "gcr.io/${PROJECT_ID}/hr-api:latest"
options:
  logging: CLOUD_LOGGING_ONLY
